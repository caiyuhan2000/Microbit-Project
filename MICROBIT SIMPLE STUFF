from microbit import *

while True:
    if button_a.is_pressed(): #looks out for if button a is being pressed right now.
        display.show(Image.HAPPY)
        
    elif button_b.is_pressed():
        display.show(Image.HEART)
       
    
    else:
        display.show(Image.SAD)
